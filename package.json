{
  "name": "travel-planner-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "dev:all": "npm --prefix frontend run dev",
    "dev:client": "npm --prefix frontend run dev",
    "dev:server": "npm --prefix backend run start",
    "build": "npm --prefix frontend run build",
    "preview": "npm --prefix frontend run preview",
    "lint": "npm --prefix frontend run lint",
    "verify:firebase": "npm --prefix frontend run verify:firebase",
    "promote-admin": "npm --prefix frontend run promote-admin",
    "firebase:login": "npm --prefix frontend run firebase:login",
    "firebase:emulators": "npm --prefix frontend run firebase:emulators",
    "firebase:deploy": "npm --prefix frontend run firebase:deploy"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
